 /***************************
  *********1.所有页面**********
  *********2.日期选择*********
  ***************************/
 layui.use('laydate', function() {
     var laydate = layui.laydate;
     //常规用法
     laydate.render({
         elem: '#timeStart'
     });
     laydate.render({
         elem: '#timeEnd'
     });
 })
 layui.use('laydate', function() {
         var laydate = layui.laydate;
         //常规用法
         laydate.render({
             elem: '#timeStartBZRZ'
         });

     })
     /***************************
      *********1.所有页面**********
      *********2.时间选择*********
      ***************************/
 layui.use('laydate', function() {
     var laydate = layui.laydate;

     //创建时间(起):
     laydate.render({
         elem: '#timeStartSFM',
         type: 'datetime'
     });
     //创建时间(止):
     laydate.render({
         elem: '#timeEndSFM',
         type: 'datetime'
     });
 });
 //  巡检点查询日期选择*********
 layui.use('laydate', function() {
     var laydate = layui.laydate;
     //创建时间(起):
     laydate.render({
         elem: '#timeStartSFMXJDCX',
         type: 'datetime'
     });
     //创建时间(止):
     laydate.render({
         elem: '#timeEndSFMXJDCX',
         type: 'datetime'
     });
 });
 //  工作日志日期选择*********
 layui.use('laydate', function() {
     var laydate = layui.laydate;
     //创建时间(起):
     laydate.render({
         elem: '#timeStartSFMGZRZ',
         type: 'datetime'
     });
     //创建时间(止):
     laydate.render({
         elem: '#timeEndSFMGZRZ',
         type: 'datetime'
     });
 });
 //  填写工作日志日期选择*********
 layui.use('laydate', function() {
     var laydate = layui.laydate;
     //创建时间(起):
     laydate.render({
         elem: '#timeStartSFMTXRZ',
         type: 'datetime'
     });
     //创建时间(止):
     laydate.render({
         elem: '#timeEndSFMTXRZ',
         type: 'datetime'
     });
 });
 //  任务指派日期选择*********
 layui.use('laydate', function() {
     var laydate = layui.laydate;
     //创建时间(起):
     laydate.render({
         elem: '#timeStartSFMRWZP',
         type: 'datetime'
     });
     //创建时间(止):
     laydate.render({
         elem: '#timeEndSFMRWZP',
         type: 'datetime'
     });
 });
 //  任务查询日期选择*********
 layui.use('laydate', function() {
     var laydate = layui.laydate;
     //执行时间(起):
     laydate.render({
         elem: '#timeStartSFMZXSJ',
         type: 'datetime'
     });
     //执行时间(止):
     laydate.render({
         elem: '#timeEndSFMZXSJ',
         type: 'datetime'
     });
 });
 //  任务查询日期选择*********
 layui.use('laydate', function() {
     var laydate = layui.laydate;
     //执行时间(起):
     laydate.render({
         elem: '#timeStartSFMJSSJ',
         type: 'datetime'
     });
     //执行时间(止):
     laydate.render({
         elem: '#timeEndSFMJSSJ',
         type: 'datetime'
     });
 });
 //  指标管理日期选择*********
 layui.use('laydate', function() {
     var laydate = layui.laydate;
     //执行时间(起):
     laydate.render({
         elem: '#timeStartSFMZBGL',
         type: 'datetime'
     });
     //执行时间(止):
     laydate.render({
         elem: '#timeEndSFMZBGL',
         type: 'datetime'
     });
 });
 //  线路管理日期选择*********
 layui.use('laydate', function() {
     var laydate = layui.laydate;
     //执行时间(起):
     laydate.render({
         elem: '#timeStartSFMXLGL',
         type: 'datetime'
     });
     //执行时间(止):
     laydate.render({
         elem: '#timeEndSFMXLGL',
         type: 'datetime'
     });
 });
 //  巡检考核评估日期选择*********
 layui.use('laydate', function() {
     var laydate = layui.laydate;
     //执行时间(起):
     laydate.render({
         elem: '#timeStartSFMXJKH',
         type: 'datetime'
     });
     //执行时间(止):
     laydate.render({
         elem: '#timeEndSFMXJKH',
         type: 'datetime'
     });
 });
 //  隐患类型分析日期选择*********
 layui.use('laydate', function() {
     var laydate = layui.laydate;
     //执行时间(起):
     laydate.render({
         elem: '#timeStartSFMYHLX',
         type: 'datetime'
     });
     //执行时间(止):
     laydate.render({
         elem: '#timeEndSFMYHLX',
         type: 'datetime'
     });
 });
 //  隐患区域分析日期选择*********
 layui.use('laydate', function() {
     var laydate = layui.laydate;
     //执行时间(起):
     laydate.render({
         elem: '#timeStartSFMYHQY',
         type: 'datetime'
     });
     //执行时间(止):
     laydate.render({
         elem: '#timeEndSFMYHQY',
         type: 'datetime'
     });
 });
 //  隐患数量分析日期选择*********
 layui.use('laydate', function() {
     var laydate = layui.laydate;
     //执行时间(起):
     laydate.render({
         elem: '#timeStartSFMYHSL',
         type: 'datetime'
     });
     //执行时间(止):
     laydate.render({
         elem: '#timeEndSFMYHSL',
         type: 'datetime'
     });
 });